# Round 2b integration status (2026-09-03)

Lanes local-backend, b10 and b11 on top of round 1 (`docs/status/round1.md`) and round 2 (`docs/status/round2.md`). Precedence: DECISIONS.md (D1-D40) > CONTRACTS.md > briefs. Run on macOS (Darwin 27.0.0), Rust 1.97.1, pnpm 11.20, Node 26, Docker present. The Rust round in `zed/` was still editing while this ran (see §3, E1).

## 1. Lanes

- **local-backend** — a no-Vercel loop on one machine: `apps/web/lib/sandbox-local.ts` (`ZS_SANDBOX_BACKEND=local`: one directory per sandbox under `ZS_LOCAL_ROOT`, loopback ports for the D21 set, `runDetached` spawns `zs-agent` with `ZS_LOCAL=1` and the `ZS_*_LISTEN` overrides, `stop` = SIGTERM + tarball snapshot, resume from snapshot, `local/<name>` repository provider with `file://` clones), `lib/dev-auth.ts` (`ZS_AUTH_MODE=dev`, loopback-only: foreign `Host`, cross-site and foreign-`Origin` requests get 403 `dev_origin_refused`), `lib/db.ts` pg-or-PGlite driver, `scripts/dev-local.sh` (build / dev / e2e / stop / clean; exports the env, never writes `.env.local`), `tests/e2e-native/{flow,local-backend,dev-auth}.test.ts`; supervisor `ZS_LOCAL`, `ZS_RPC_LISTEN`/`ZS_HEALTH_LISTEN`/`ZS_LOCAL_API_LISTEN`/`ZS_SERVER_CONTROL_LISTEN`/`ZS_PROXY_BIND_IP`, `relocate_workspace_dir`, `lsof` port fallback, `port_held_by_foreign_process`; `zed/crates/remote/tests/native_e2e.rs` (real `WebSocketConnectionOptions` + `RemoteClient`: Hello/HelloAck, `AddWorktree`, `OpenBufferByPath`, `UpdateBuffer`, `SaveBuffer`).
- **b10** — devcontainer image builder: `apps/web/lib/devcontainer/*` (JSONC parse, normalize, Feature allowlist, substitution, env filter, `configHash`), `lib/{images,oci,vcr,builds}.ts` (image choice, admission control, VCR readiness/gc), `workflows/build-devcontainer-image.ts` (builder VM per D35, deleted as soon as the driver exits, optional chained prebuild), `sandbox/image/builder/{Dockerfile,zs-build-devcontainer.sh,layer/}` (D36 inverted layering, `zs-layer-fixup.sh`), `sandbox/image/{build-builder.sh,scripts/vcr-wait-ready.mjs}`, contracts `docs/contracts/{manifest-devcontainer.v1.json,devcontainer-normalized.v1.json}`; supervisor `manifest.devcontainer` (`DevcontainerSpec`, D38 authority, D37 `onCreate → updateContent → postCreate`) and `Phase::Services` (`dockerd`, D40); decisions D35-D40 recorded.
- **b11** — AI provider proxy: `app/api/ai/{[provider]/[[...path]],keys,keys/[...provider],usage}/route.ts`, `lib/ai/{auth,cookie,errors,headers,keys,providers,proxy,schemas,usage}.ts` (`zs_ai` cookie auth with cross-site refusal and no Clerk fallback, per-user keys as AES-256-GCM envelopes in `ai_keys` with per-repo `ai_key_exports`, provider table `docs/contracts/ai-providers.v1.json`, `proxyAiRequest` streaming pass-through with `ZS_AI_*` body/timeout/stream caps and abort propagation, Redis-reserved daily/monthly/in-flight caps + `ai_usage` ledger), `proxy.ts` keeps `api/ai/` outside the Clerk matcher; `tests/ai/*` (8 files).
- **integrator** — no code changes were needed: the three lanes lint, typecheck, and test together as landed. Files written: this page only.

## 2. Verification

| # | Command (cwd) | Result |
|---|---|---|
| 1 | `pnpm lint` (`apps/web`) | PASS (exit 0) |
| 2 | `pnpm exec tsc --noEmit` (`apps/web`) | PASS (exit 0) |
| 3 | `pnpm test` (`apps/web`) | PASS: 66 files passed, 1 skipped (`e2e-native/flow` without `ZS_E2E_BASE_URL`); 552 tests passed, 2 skipped; 71 s (round 1: 46 files / 362 tests) |
| 4 | `cargo clippy --all-targets -- -D warnings` (`sandbox/supervisor`) | PASS (`Finished`, exit 0) |
| 5 | `cargo test` (`sandbox/supervisor`) | PASS: 132 unit (round 1: 100) + `api` 5, `log_shipper` 6, `proto_tags` 2, `proxy_roundtrip` 6, `warm_client` 1; `port_watcher_live` 0 (Linux only); `start_against_fake_server` 8 and `warm_against_serve` 1 env-gated (see row 7) |
| 6 | `./scripts/dev-local.sh e2e` (`apps/web`) | **PASS on the first run** (exit 0, 10:34:09-10:37:41 UTC; docker Postgres `zs_e2e_20260903063445`, `next dev` on 127.0.0.1:3100). Flow: fixture `local/e2e-5b401cbc` → `POST /api/workspaces` 202 (`ws_RAA7ANJSQS8GE58XSG2H`) → `creating/boot:manifest` → `running/boot:clone` in 12 s (supervisor pid 35072, server pid 35153, rpc `ws://127.0.0.1:55486/rpc`) → `/connect` D26 shape (`con_6R7G2GJXD9E7SYXH3P43`, claims `ws`/`sid`/`aud`/`iss` checked) → native client `HelloAck` (`build c3cf80c0…`, `resumed false`, `epoch 117206660181917697`), `AddWorktree` → 5 entries, `OpenBufferByPath`/`UpdateBuffer`/`SaveBuffer` → README.md on disk = before + append (86 bytes), `git status` dirty → `POST /stop` → `stopped/user` in 3 s, supervisor and server exited, snapshot `lsnap_…_mtle5dn5.tgz` → `DELETE` 202 → 410 and the sandbox directory gone. vitest: 3 files / 14 tests passed in 168 s; `native_e2e`: 1 passed. Nothing left behind: no `zs-agent`/`zed-remote-server`/`next dev` process, `$TMPDIR/zs-local/{,repos}` empty, e2e database dropped. |
| 7 | `ZS_RUN_START_INTEGRATION=1 cargo test --test start_against_fake_server` (`sandbox/supervisor`) | PASS: 8 passed in 68 s against the real `zs-agent` (boot/clone/serve/stop, stale-server sweep, rebuild tarball digest, stop directive, SIGTERM mid-clone, crash-loop trip, prebuild, unavailable manifest → exit 3) |

## 3. Gaps

| # | Gap | Owner |
|---|---|---|
| E1 | The e2e's `cargo build -p remote_server` (and the script's `--no-run` prebuild of `native_e2e`) failed at 10:34 with `error[E0432]: unresolved import web_time` in `crates/proto` — the concurrent Rust round had switched `proto` to `web-time` (R3-4) 20 s before adding the dependency to `crates/proto/Cargo.toml` (06:34:41 local). The script fell back, as designed, to the stashed servable binary `zed/target/zs-local/zed-remote-server` (built 04:44, build id `c3cf80c0…`), and the native client compiled cleanly two minutes later once the tree was consistent (`Finished test profile in 2m 19s`, most of it waiting on the shared cargo lock). So the pass is against a server binary from before round 2's b4/b5 landings, not a fresh one; re-run `scripts/dev-local.sh e2e` once the zed round is quiet to refresh the stash. Not a lane defect; no file was changed. | integrator (re-run) |
| E2 | `next dev` logs `Module not found: Can't resolve '../drizzle'` from `lib/db.ts:98` (`new URL("../drizzle", import.meta.url)`, a Turbopack static-analysis warning; the cwd fallback is what actually resolves). Harmless, but it prints on every route compile; wrap the URL in a non-literal or drop the branch. | local-backend |
| E3 | b10 is unit-tested only: no devcontainer image was built here (needs Docker on Linux + VCR credentials, `sandbox/image/build-builder.sh`); `Phase::Services` (`dockerd`) is exercised only by the supervisor's unit tests. Round-1 G7 (image job, `port_watcher_live`) still stands. | b10, b8 |
| E4 | b11 is tested against in-process fake upstreams only; no request to a real provider (Anthropic/OpenAI/…) has been made from this repo, and the editor-side consumer (`zed_web` pointing model providers at `/api/ai/*`) is R3-3 work. | b11, b7 |
| E5 | The native e2e covers create → connect → `/rpc` → edit/save → stop → delete on the local backend; a Vercel sandbox run (round-1 G8 / round-2 R3-7) is still open, and the e2e does not yet exercise stop → resume (D28 terminal recreation) or a browser client. | local-backend (driver), b9 |
